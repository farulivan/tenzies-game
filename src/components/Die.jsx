const Die = (props) => {
    return (
        <div className="number bg-emerald-400 rounded-lg drop-shadow-md h-11 text-xl flex items-center justify-center">{props.value}</div>
    )
}

export default Die